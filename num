from typing import TextIO

def binary_search (sequence, key):
    low = 0
    high = len(sequence) - 1
    while low <= high:
        mid = (low + high)//2
        print (mid)
        if sequence[mid] > key:
            high = mid - 1
        elif sequence [mid] < key:
            low = mid + 1
        else:
            return mid
    return None
binary_search([1,1,3,4,6,7,8,8,9],3)
